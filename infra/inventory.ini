[runners]
pxe-github-runners ansible_host=192.168.4.105 ansible_user=root ansible_ssh_private_key_file=~/.ssh/id_rsa
germany ansible_host=193.24.209.222 ansible_user=root ansible_ssh_private_key_file=~/.ssh/id_rsa

# Connection details live in host_vars/<host>.yml so each Mac is addressed
# explicitly. They used to be ansible_connection=local here for both, which
# silently applied every host's runner list to whichever machine ran the
# playbook — see the note in host_vars/mac-mini.yml.
[macos_runners]
mac-mini
mac-laptop
