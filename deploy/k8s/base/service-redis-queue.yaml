apiVersion: v1
kind: Service
metadata:
  name: spanbarn-redis-queue
spec:
  selector:
    app.kubernetes.io/name: spanbarn-redis-queue
  ports:
    - port: 6379
      targetPort: 6379
      name: redis
