# The build context is the repo root. A local pnpm install leaves symlinked
# node_modules and build output in the workspace members; copying them over
# the image's own install would shadow it.
**/node_modules
web/dist
sdks/js/dist
**/coverage
.git
